type Manager @entity {
  id: ID!
  address: String!
  setTokens: [SetToken!]! @derivedFrom(field: "manager")
}

type SetToken @entity {
  id: ID!
  name: String!
  symbol: String!
  address: String!
  inception: BigInt!
  manager: Manager!
}

type SetTokenCount @entity {
  id: ID!
  count: BigInt!
}
