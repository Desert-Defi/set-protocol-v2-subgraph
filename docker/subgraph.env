# Node version to use for deployment
NODE_VER=16

# Subgraph version label to deploy
# (See: https://thegraph.com/docs/en/studio/deploy-subgraph-studio#deploying-a-subgraph-to-subgraph-studio)
SUBGRAPH_VER_LABEL=test-0.0.1

# HOSTED SERVICE DEPLOYMENT PARAMS
# --------------------------------
IPFS_HOSTED=api.thegraph.com/ipfs/
NODE_HOSTED=api.thegraph.com/deploy/
# Specify the external network to deploy on (see deployments.json for options)
NETWORK=mainnet

# LOCAL DEPLOYMENT PARAMS
# -----------------------
ACCESS_TOKEN=test
IPFS_LOCAL=docker-ipfs-1
IPFS_PORT=5001
NODE_LOCAL=docker-graph-node-1
NODE_PORT=8020

